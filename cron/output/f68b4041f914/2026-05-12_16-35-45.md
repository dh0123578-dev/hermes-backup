# Cron Job: 企业微信网关健康检查

**Job ID:** f68b4041f914
**Run Time:** 2026-05-12 16:35:45
**Schedule:** every 120m

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

检查企业微信网关是否存活。读取 ~/.hermes/gateway_state.json 中的 pid，用 ps -p $pid 检查进程是否存在。如果进程不存在，用 python -m hermes_cli.main gateway run 在后台重启网关。如果进程存在但 wecom 状态不是 connected，也重启网关。告知用户检查结果和重启情况。

## Response

Everything checks out:

1. **Process**: PID 778821 is alive and running (`ps` shows S = sleeping normally)
2. **Gateway state**: `"running"` — confirmed
3. **WeCom state**: `"connected"` — confirmed in both `gateway_state.json` and the state database
4. **Feishu state**: `"connected"` — also confirmed
5. **Process resources**: Has open file descriptors for state.db, sockets, and cron lock — all normal

No restart needed. The gateway and its WeCom platform are both healthy.

**企业微信网关健康检查结果** ✅

| 检查项 | 状态 |
|--------|------|
| **进程 (PID 778821)** | ✅ 存活 (python -m hermes_cli.main gateway run --replace) |
| **Gateway 状态** | ✅ running |
| **WeCom 状态** | ✅ connected |
| **飞书状态** | ✅ connected |
| **运行时间** | 自 5月10日 起持续运行中 |

一切正常，无需重启。
