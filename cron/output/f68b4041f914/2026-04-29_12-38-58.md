# Cron Job: 企业微信网关健康检查

**Job ID:** f68b4041f914
**Run Time:** 2026-04-29 12:38:58
**Schedule:** every 30m

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

检查企业微信网关是否存活。读取 ~/.hermes/gateway_state.json 中的 pid，用 ps -p $pid 检查进程是否存在。如果进程不存在，用 python -m hermes_cli.main gateway run 在后台重启网关。如果进程存在但 wecom 状态不是 connected，也重启网关。告知用户检查结果和重启情况。

## Response

No errors or disconnections in the wecom connection. The logs only show routine "RAW MSG" warnings (informational) and WebSocket pings being received normally. The last wecom message was received today at 07:29, and pings continue every 30 seconds.

Everything is healthy. No restart needed.

## 企业微信网关检查报告

**✅ 网关运行正常，无需重启**

| 检查项 | 状态 |
|--------|------|
| **进程 PID** | 1058120 — 正常运行中（已运行约 21 小时） |
| **进程状态** | `Sl`（多线程，正常运行） |
| **WebSocket 心跳** | 每 30 秒收到一次 ping，`errcode: 0` 正常响应 |
| **WeCom 连接状态** | `connected`（自昨日 14:55 起持续连接） |
| **飞书连接状态** | `connected` |
| **最后收到消息** | 2026-04-29 07:29（约 5 小时前收到文本消息） |
| **错误日志** | 无 wecom 相关错误或断连记录 |

**详情：**
- 网关主进程 PID 1058120 存活，含 10 个工作线程
- 企业微信 WebSocket 连接正常，持续收到服务端心跳确认（`errcode: 0`），无异常
- 飞书平台同样处于 connected 状态
- 最近 WeCom 消息（文本、语音）均已正常接收处理
